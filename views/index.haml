.dt-logo.clearfix
  %img.pull-right{:src => '/img/discover/logo.png'}
.dt-index-bg.row-fluid
  .span5
    - @audiences.each do |audience|
      %a.dt-audience{:href => "/#{audience.slug}"}
        = Kramdown::Document.new(audience.description).to_html
